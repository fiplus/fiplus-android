<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    >
    <ImageView
        android:id="@+id/pending_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/desc_list_item_icon"
        android:background="@android:drawable/ic_menu_recent_history"
        android:backgroundTint="#fff8a426" />

    <TextView
        android:id="@+id/pending_suggestion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Large Text"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:textColor="@color/counter_text_bg"
        android:layout_centerVertical="true"
        android:textStyle="italic"
        android:layout_toRightOf="@+id/pending_icon"
        android:layout_toEndOf="@+id/pending_icon"
        android:layout_toLeftOf="@+id/delete_pending"/>

    <ImageView
        android:id="@+id/delete_pending"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:contentDescription="@string/desc_list_item_icon"
        android:background="@android:drawable/ic_delete"
        />

</RelativeLayout>